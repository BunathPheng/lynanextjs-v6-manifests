---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lynanextjs-v6                  # Ingress name
  labels:
    app: lynanextjs-v6
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "lynanextjs-v6.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lynanextjs-v6               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
